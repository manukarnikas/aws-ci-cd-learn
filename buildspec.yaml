version: 0.2

cache:
  paths:
    - '/root/.npm/**/*'

phases:
  install:
    commands:
      - npm ci

  build:
    commands:
      - npm run build
      - cp index.html dist/

artifacts:
  files:
    - '**/*'
  base-directory: dist